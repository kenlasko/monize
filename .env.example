# Environment Configuration
NODE_ENV=development

# Database Configuration
POSTGRES_DB=moneymate_db
POSTGRES_USER=moneymate_user
POSTGRES_PASSWORD=changeme_in_production

# JWT Configuration
# REQUIRED: Generate a secure secret (minimum 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=7d

# OIDC Configuration (Optional - for OpenID Connect authentication)
# Supports: Pocket-ID, Authentik, Authelia, and any standard OIDC provider
# Leave these empty to disable OIDC and use only local authentication
#
# OIDC_ISSUER_URL: Your OIDC provider's base URL (NOT the .well-known URL)
# Examples:
#   Pocket-ID:  https://pocket-id.example.com
#   Authentik:  https://auth.example.com/application/o/your-app/
#   Authelia:   https://auth.example.com
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
# Callback URL must point to the backend API
OIDC_CALLBACK_URL=http://localhost:3001/api/v1/auth/oidc/callback

# Local Authentication
# Set to 'false' to disable local email/password authentication
# Useful when you want OIDC-only authentication
LOCAL_AUTH_ENABLED=true

# API Keys
# Exchange Rate API - Get free key from https://exchangeratesapi.io/ or https://fixer.io/
EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key

# Stock API - Get free key from https://www.alphavantage.co/ or https://finnhub.io/
STOCK_API_KEY=your-stock-api-key

# Application URLs
# INTERNAL_API_URL: Backend URL for server-side communication (set in docker-compose/k8s)
# PUBLIC_APP_URL: Public frontend URL (for OAuth callbacks, CORS)
PUBLIC_APP_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password
EMAIL_FROM=noreply@moneymate.app

# Session Configuration
SESSION_SECRET=your-session-secret-change-in-production

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
